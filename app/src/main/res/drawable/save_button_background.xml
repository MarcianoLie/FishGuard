<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#0056b3"/> <!-- Warna biru gelap saat ditekan -->
            <corners android:radius="8dp"/>
        </shape>
    </item>

    <!-- Disabled State -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="#A0A0A0"/> <!-- Abu-abu saat tombol tidak aktif -->
            <corners android:radius="8dp"/>
        </shape>
    </item>

    <!-- Normal State -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#007AFF"/> <!-- Warna biru cerah default -->
            <corners android:radius="8dp"/> <!-- Sudut membulat -->
        </shape>
    </item>
</selector>